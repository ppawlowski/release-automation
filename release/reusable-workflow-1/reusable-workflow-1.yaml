name: Checkout

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Print something
        uses: actions/github-script@v7.0.1
        with:
          script: |
            console.log("Hello from reusable workflow 1!")
