# release-automation

Basic Node.js starter project.

## Requirements

- Node.js 20+ (recommended)

## Getting started

1. Install dependencies (none for now):

```bash
npm install
```

2. Run the app:

```bash
npm start
```

3. Run in watch mode during development:

```bash
npm run dev
```